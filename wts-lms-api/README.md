# WTS LMS API

Minimal Phoenix-compatible Mix scaffold for the WTS Canvas replacement API.

This scaffold keeps external dependencies empty so `mix test` can run before
Phoenix, Ecto, Oban, S3, and email packages are selected in later tasks. Config
placeholders are present in `config/config.exs` and test-specific settings live
in `config/test.exs`.
