# WTS LMS Web

Minimal Vite-compatible React/TypeScript SPA scaffold for the WTS Canvas
replacement UI. The generated API client placeholder lives at `src/api/client.ts`
until the OpenAPI contract generation task wires the real client.
